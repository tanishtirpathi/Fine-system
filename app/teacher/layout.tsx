import CollegeShell from "@/components/college/college-shell";

export default function TeacherLayout({ children }: { children: React.ReactNode }) {
  return <CollegeShell>{children}</CollegeShell>;
}
